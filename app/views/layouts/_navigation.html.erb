<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="btn btn-navbar" data-target=".nav-collapse" data-toggle="collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </a>
      <%= link_to 'ICBH', root_path, :class => "brand" %>
      <div class="nav-collapse collapse">
        <ul class="nav">
          <li class="dropdown">
            <a href="#" class="dropdown" data-toggle="dropdown">Photos</a>
            <ul class="dropdown-menu">
              <li><%= link_to "All", photos_path %></li>
              <li>
              <% tag_cloud Photo.tag_counts, %w[s m l] do |tag, css_class| %>
                <%= link_to tag.name, tag_path(tag.name), class: css_class %>
              <% end %>
              </li>
            </ul>
          </li>
          <li><%= link_to "About", about_path %></li>
          <li><%= link_to "Stimulus", stimulus_path %></li>
          <li><a href="http://itcouldbehonolulu.tumblr.com/">Blog</a></li>
        </ul>
        <ul class="nav pull-right">
          <li>
          <% if admin_signed_in? %>
            <%= link_to('New Photo', new_photo_path, :class => 'pull-right') %>
          <% end %>
          </li>
        </ul>
      </div><!--/.nav-collapse -->
    </div><!--/.container -->
  </div><!--/.navbar-inner -->
</header><!--/.header -->